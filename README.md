# Mmbar-teame
این اسکریپت برای ثبت درخواست برای عضویت در تیم ما هست ...
کد ها در کانال قرار دارد اول با دستور
```
apt update && apt upgrade -y
pkg install git python3 -y
pip install -r modluse.txt
python3 start.py

```
